<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE plist PUBLIC "-//Apple//DTD PLIST 1.0//EN" "http://www.apple.com/DTDs/PropertyList-1.0.dtd">
<plist version="1.0">
<dict>
  <!-- Electron / V8 / Node need JIT and unsigned executable memory -->
  <key>com.apple.security.cs.allow-jit</key>
  <true/>
  <key>com.apple.security.cs.allow-unsigned-executable-memory</key>
  <true/>

  <!-- Required so native modules (keytar, ffmpeg, ffprobe) load under
       hardened runtime. Without this they fail with "library validation". -->
  <key>com.apple.security.cs.disable-library-validation</key>
  <true/>

  <!-- ffmpeg/ffprobe rely on DYLD env vars for dylib resolution -->
  <key>com.apple.security.cs.allow-dyld-environment-variables</key>
  <true/>

  <!-- Outbound HTTPS to the Cloudflare Worker / OpenAI -->
  <key>com.apple.security.network.client</key>
  <true/>

  <!-- User-selected file access — for picking + renaming clip files -->
  <key>com.apple.security.files.user-selected.read-write</key>
  <true/>
</dict>
</plist>
